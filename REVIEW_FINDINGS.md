# Review Findings

> **Verifikations-Pass (2026-07-20):** Alle Findings mit Severity **kritisch/hoch** wurden gegen den tatsächlichen Code geprüft. Verworfene bzw. korrigierte Findings sind in der jeweiligen Zeile mit **VERWORFEN** / **TEILWEISE VERWORFEN** und einer Begründung markiert. Die konsolidierte, priorisierte Auswertung steht in **`REVIEW_REPORT.md`**.

| Severity | Datei:Zeile | Code-Zitat | Problem | Fix-Vorschlag |
|----------|-------------|------------|---------|----------------|
| MEDIUM | server/currency.js:99-104 | `const current = await getBalanceMemory(playerName); const newBalance = normalizeMoney(current + amount); balances.set(playerName, newBalance);` | `addBalance()` im In-Memory-Fallback (kein `DATABASE_URL`) liest `current` und schreibt `newBalance` OHNE `withBalanceLock`, während `deductBalance()` (Z.172-179) und `addDiamonds()` (Z.283-288) im selben Modul denselben Lock-Mechanismus konsequent nutzen. Zwei gleichzeitige `addBalance`-Aufrufe für denselben Spieler (z.B. zwei parallele Gewinnauszahlungen) können beide denselben `current`-Wert lesen und sich gegenseitig überschreiben ("lost update") — eine Auszahlung geht silently verloren. Da `await getBalanceMemory()` einen Event-Loop-Tick durchläuft, ist die Race real erreichbar, sobald zwei Socket-Events für denselben Spieler quasi gleichzeitig eintreffen. | `addBalance` im Memory-Zweig ebenfalls mit `withBalanceLock(playerName, async () => {...})` umschließen, analog zu `deductBalance`. |
| HIGH | server/currency.js:243-257 | `const currentBalance = await getBalanceMemory(playerName); if (cost > currentBalance) return null; ... balances.set(playerName, newBalance); ... diamonds.set(playerName, newDiamonds);` | `buyDiamonds()` im In-Memory-Fallback macht einen kompletten Read-Check-Write über zwei Maps (`balances`, `diamonds`) OHNE jeglichen Lock. Zwei parallele `buyDiamonds`-Aufrufe für denselben Spieler können beide denselben `currentBalance` lesen, beide die `cost > currentBalance`-Prüfung bestehen und beide abziehen — das Ergebnis ist eine negative Balance (Double-Spend) und doppelte Diamant-Gutschrift, obwohl der Spieler nur einmal genug Guthaben hatte. Genau das Muster, vor dem `withBalanceLock` in `deductBalance` schützt, fehlt hier. | Den gesamten Memory-Zweig von `buyDiamonds` in `withBalanceLock(playerName, async () => {...})` einwickeln, wie es `deductBalance`/`addDiamonds` bereits tun. |
| MEDIUM | server/identity.js:40-44 | `async function setOwnerInDb(playerName, ownerToken) { if (!isDatabaseEnabled()) { memoryOwners.set(playerName, ownerToken); return true; } }` | Im In-Memory-Fallback ist `setOwnerInDb` ein bedingungsloses `Map.set` — anders als der DB-Zweig darunter, der per `where owner_token is null` ein TOCTOU-sicheres conditional update macht. Zwei parallele `claimName`-Aufrufe für denselben, bisher unclaimten Namen mit unterschiedlichen Owner-Tokens lesen in `getOwnerFromDb` beide `null`, und `setOwnerInDb` überschreibt dann einfach den zuvor gesetzten Token des jeweils anderen Callers ("last write wins" statt "first claim wins"). Der zweite Aufruf gibt fälschlich `{ ok: true, firstClaim: true }` zurück, obwohl der Name inzwischen schon vom ersten Token geclaimt wurde — ein Name kann im Memory-Modus von zwei Browsern gleichzeitig "erfolgreich" geclaimt werden, was dem TOFU-Sicherheitsziel widerspricht. | `setOwnerInDb` im Memory-Zweig analog zum DB-Zweig bedingt machen, z.B. `if (memoryOwners.has(playerName)) return false; memoryOwners.set(playerName, ownerToken); return true;` |
| MEDIUM | server/achievements.js:326-330 | `if (await isAlreadyUnlocked(...)) continue; const inserted = await recordUnlock(...); if (inserted) { const rewards = await grantReward(playerName, a); ... }` | `recordUnlock` (eigene DB-Query, sofort committed) und `grantReward` → `addBalance`/`addDiamonds` (jeweils eigene, separate `withTransaction`) laufen NICHT in derselben Transaktion. Schlägt die Reward-Vergabe nach erfolgreichem `recordUnlock` fehl, bleibt der Achievement-Unlock dauerhaft in der DB stehen, aber die Belohnung wurde nie gutgeschrieben. Da `isAlreadyUnlocked` danach `true` liefert, kann die Belohnung auch nie nachträglich nachgeholt werden — der Spieler verliert sie endgültig. | `recordUnlock` und `grantReward` in eine gemeinsame Transaktion einbetten (`client` durchreichen), damit ein Fehler in der Belohnungsvergabe auch den Unlock zurückrollt. |
| LOW | server/daily-streak.js:188-213 | `// Reward credit happens outside this UPDATE so the streak row is committed even if the wallet write fails. return { ok: true, ..., _pending: true }; }).then(async (res) => { ... newBalance = await addBalance(...); }` | Bewusst dokumentierter Trade-off: der Streak-Zähler wird in einer eigenen Transaktion committed, die Coin-/Diamond-Gutschrift läuft danach separat. Schlägt die Gutschrift fehl, bleibt `last_claimed_day` auf "heute geclaimt" stehen und der Spieler kann den Claim für diesen Tag nicht wiederholen — die Belohnung ist unwiderruflich verloren, obwohl `ok: true` zurückgegeben wird. Kein Double-Spend/negative Balance, aber ein stiller Reward-Verlust im Fehlerfall. | Fehlerfall im `.then()`-Handler abfangen und bei Fehlschlag loggen + Warnung statt stillem `newBalance: null` zurückgeben, oder Reward-Gutschrift in einen Retry-/Ledger-Mechanismus überführen. |
| LOW | server/index.js:210-218 | `if (GAME_ENABLED && stocksRouter.fetchTickerQuotes) { setInterval(() => { stocksRouter.fetchTickerQuotes()...  }, 5 * 60 * 1000); }` | Der Ticker-Background-Refresh-`setInterval` wird nirgends referenziert/gecleart. `gracefulShutdown()` ruft weder `clearInterval` dafür auf, noch existiert ein Handle dafür. Aktuell unschädlich (hartes `process.exit(0)`), aber bei erneutem Modul-Load (Tests, Multi-Instanz) akkumulieren sich nicht bereinigte Intervalle. | Interval-Handle in einer Modulvariable speichern und in `gracefulShutdown` per `clearInterval` stoppen, konsistent mit `stopMatchChecker()`/`stopKeepAlive()`. |
| LOW | server/portfolio-history.js:8,18-20 | `const history = new Map(); ... if (!history.has(playerName)) { history.set(playerName, []); }` | Der äußere `history`-Map (playerName → Snapshot-Array) hat KEINE Eviction — jeder Eintrag bleibt für die gesamte Prozesslaufzeit bestehen, auch wenn der Spieler nie wieder aktiv ist. Nur das innere Array pro Spieler ist auf `MAX_SNAPSHOTS = 100` begrenzt. Bei vielen unterschiedlichen (auch kurzlebigen/Test-)Spielernamen wächst der Map unbegrenzt — ein klassischer langsamer Memory Leak, den der periodische Cleanup in `server/cleanup.js` nicht erfasst. | TTL/LRU ergänzen (z.B. `lastAccess`-Zeitstempel pro Spieler und periodische Bereinigung alter Einträge), oder History in die DB auslagern. |
| HIGH | server/turkish-streaks.js:77-119 | `const current = await client.query('select ... from turkish_streaks where player_id = $1', ...); ... const { currentStreak, alreadyCompleted } = computeNextStreak(...); ... await client.query('update turkish_streaks set ... where player_id = $4', ...); ... if (rewardCoins > 0) { await addBalance(...); }` | Klassisches TOCTOU-Muster im DB-Modus: `SELECT` liest `last_completed_day`, die Anwendung entscheidet in JS ob `alreadyCompleted`, danach folgt ein einfaches `UPDATE ... where player_id = $4` OHNE Bedingung auf den zuvor gelesenen Zustand (kein `WHERE last_completed_day IS DISTINCT FROM $3`, kein `SELECT ... FOR UPDATE`). Zwei parallele `POST /api/turkish/complete`-Requests für denselben Spieler (Doppelklick, Retry, zwei Tabs) lesen unter READ COMMITTED denselben `last_completed_day`, berechnen beide `alreadyCompleted: false` und zahlen beide die Streak-Belohnung aus — der Spieler kassiert den Tages-Reward doppelt. Anders als das strukturell identische `server/daily-streak.js` (atomares `UPDATE ... where ds.last_claimed_day is null or ds.last_claimed_day < $2 returning ...`) fehlt hier der bedingte Atomic-Update-Guard komplett. | Das `UPDATE` bedingt machen und über `returning` prüfen, ob es gegriffen hat, analog zu `server/daily-streak.js`; bei leerem Ergebnis `{ ok: true, alreadyCompleted: true, rewardCoins: 0 }` zurückgeben statt der optimistischen Read-Then-Write-Logik. |
| LOW | server/lol-betting.js:6-39 | `const betsMemory = []; let nextBetId = 1; ... betsMemory.push(bet);` | Im In-Memory-Fallback wächst `betsMemory` unbegrenzt — abgelaufene/resolved/refunded Bets werden nie aus dem Array entfernt, nur ihr `status`-Feld ändert sich. Alle Lookup-Funktionen iterieren linear über das gesamte Array, was zusätzlich zur Speicher- auch zur CPU-Last mit der Zeit wird. | Resolved/refunded Bets nach einer Aufbewahrungsfrist aus `betsMemory` entfernen (z.B. aus dem periodischen Cleanup in `server/cleanup.js` heraus), oder auf eine Map mit expliziter Eviction umstellen. |
| HIGH | server/handlers/blackjack.js:159-190 | `const existing = games.get(player.name); if (existing && !existing.finished) {...return;} ... const balance = await deductBalance(player.name, bet, ...); ... games.set(player.name, g);` | `bj-deal` prüft `existing`-Game synchron, deduziert dann den Einsatz per `await deductBalance` (Await-Punkt) und schreibt das neue Game-Objekt erst danach in die `games`-Map. Socket.IO wartet beim Verarbeiten eingehender Events NICHT auf das Resolven vorheriger async Handler desselben Sockets — feuert ein Client (Doppelklick, Netzwerk-Retry, manipulierter Client) zwei `bj-deal` Events kurz hintereinander, sieht der zweite Aufruf beim `games.get`-Check noch keinen `existing`-Eintrag (der erste hat ihn ja erst nach seinem eigenen `await` gesetzt), passiert die Prüfung ebenfalls, deduziert erneut einen Einsatz und überschreibt mit `games.set` das erste Game-Objekt. Der erste Einsatz ist damit unwiederbringlich verloren (abgebucht, aber kein zugehöriges Game mehr vorhanden, das ihn je auszahlen könnte). | Reentrancy-Schutz einführen: z.B. sofort (synchron, vor dem `await deductBalance`) einen Platzhalter/Lock in `games.set(player.name, { pending: true })` schreiben, damit ein zweites `bj-deal` im selben Fenster die "existing"-Prüfung fehlschlagen lässt; Lock erst nach erfolgreichem Deal oder nach Fehlschlag (mit Rückbuchung) wieder freigeben. |
| HIGH | server/handlers/brain-versus.js:402-462 | `socket.on('brain-versus-leave', async () => {... if (room.game) { ... const newBalance = await addBalance(opponent.name, 20, ...); ... room.game = null; } ...});` / `export async function cleanupBrainVersusOnDisconnect(socket, room, io) { if (room.gameType === 'strictbrain' && room.game) { ... await addBalance(opponent.name, 20, ...); ... room.game = null; } }` | Beide Pfade — expliziter `brain-versus-leave`-Event und der Disconnect-Cleanup-Hook `cleanupBrainVersusOnDisconnect` — prüfen `if (room.game)` synchron und setzen `room.game = null` erst NACH einem `await addBalance(...)`. Sendet ein Client `brain-versus-leave` und trennt die Verbindung (Tab-Schließen, Netzwerkabbruch) fast gleichzeitig — realistisch, da beide Aktionen typischerweise im selben Moment ausgelöst werden — können beide Handler das `if (room.game)` VOR dem jeweils anderen `await addBalance` durchlaufen, da `room.game` erst nach Abschluss des jeweiligen Awaits genullt wird. Der Gegner erhält dadurch die 20 SC Forfeit-Belohnung doppelt und `brain-versus-result` wird zweimal an ihn gesendet. | Synchron VOR dem `await addBalance` ein Flag setzen (z.B. `room.game.forfeited = true` oder `room.game = null` bereits vor dem Await, mit dem Game-Objekt separat für die Auszahlung zwischengespeichert), damit der zweite Pfad (Leave oder Disconnect-Cleanup, je nachdem was zuerst läuft) den bereits verarbeiteten Forfeit erkennt und überspringt. |
| HIGH | server/handlers/crash.js:277-329 | `if (round.bets.has(player.name)) {...return;} ... const balanceAfterBet = await deductBalance(player.name, bet, ...); ... round.bets.set(player.name, {...});` | Gleiches TOCTOU-Muster wie bei `bj-deal`: Der Duplikat-Check `round.bets.has(player.name)` läuft synchron VOR dem `await deductBalance`. Der Handler prüft zwar bereits explizit gegen eine Round-ID-Verschiebung (Kommentar Z.306-308 "Refund + bail if the round flipped"), aber NICHT gegen einen zweiten `crash-bet` desselben Spielers in derselben Runde, der während desselben Await-Fensters eintrifft. Feuert der Client zwei `crash-bet`-Events kurz hintereinander, sehen beide `round.bets.has()` noch `false` (das erste Event hat den Eintrag noch nicht gesetzt), beide deduzieren den Einsatz, und `round.bets.set` beim zweiten Event überschreibt den ersten Bet-Eintrag komplett — der erste abgebuchte Einsatz ist an keinem Bet-Objekt der Runde mehr hängt und kann nie ausgezahlt werden (stiller Geldverlust für den Spieler, "Geist-Einsatz"). | Wie bei `bj-deal`: synchron VOR dem `await deductBalance` einen Platzhalter in `round.bets.set(player.name, { pending: true })` reservieren, damit ein zweiter `crash-bet` im selben Fenster durch den `has()`-Check abgewiesen wird; bei fehlgeschlagenem Deduct den Platzhalter wieder entfernen. |
| MEDIUM | server/handlers/blackjack.js:240-263 | `if (g.playerHand.length !== 2) {...return;} const balanceAfterDouble = await deductBalance(player.name, g.bet, ...); ... g.doubled = true; g.bet = g.bet * 2; g.playerHand.push(deal());` | Analoge Race wie bei `bj-deal`: Die Prüfung `playerHand.length !== 2` läuft synchron vor dem `await deductBalance`. Feuert derselbe Spieler zwei `bj-double`-Events kurz hintereinander, sehen beide Aufrufe beim Eintritt noch `playerHand.length === 2` (der erste hat die Karte erst nach seinem `await` gezogen), beide deduzieren den Einsatz, und beide verdoppeln/ziehen eine Karte — das Blatt bekommt zwei zusätzliche Karten und der Einsatz wird effektiv vervierfacht statt verdoppelt, mit doppelt abgebuchtem Betrag für ein Double, das nur einmal gültig sein sollte. | Denselben Lock-Ansatz wie bei `bj-deal` anwenden: `g.doubled` (oder ein eigenes `g.locked`-Flag) synchron VOR dem `await deductBalance` setzen und bei fehlgeschlagenem Deduct wieder zurücksetzen, damit ein zweiter gleichzeitiger `bj-double`-Aufruf sofort abgewiesen wird. |
| HIGH | server/handlers/maexchen.js:17-18,65-75 | `if (!room || room.game) return;` ... `room.bets[socket.id] = amount;` | Der `room.game`-Check in `place-bet` erfolgt nur vor dem `await withWallet(...)`. Läuft `place-bet` parallel zu `start-game` (Host startet, während ein Einsatz noch in der DB-Transaktion hängt), wird der Betrag trotzdem abgebucht und `room.bets` nach Spielstart noch geschrieben — der Pot in `start-game` wurde aber bereits ohne diesen Betrag berechnet. Der Spieler verliert Coins, die in keinem Pot landen. | Nach dem `await withWallet(...)` erneut `room.game` prüfen; falls das Spiel inzwischen gestartet wurde, den Betrag zurückerstatten statt `room.bets` zu setzen. |
| HIGH | server/handlers/maexchen.js:30-31,49-65 | `const oldBet = room.bets[socket.id] || 0; if (amount === oldBet) return;` | `oldBet` wird vor dem `await withWallet` gelesen. Sendet derselbe Client zweimal denselben Betrag sehr schnell hintereinander, lesen beide Handler-Aufrufe `oldBet=0` (da `room.bets[socket.id]` noch nicht geschrieben wurde), beide bestehen die `amount===oldBet`-Prüfung und beide deduzieren den Betrag; `room.bets[socket.id]` wird am Ende nur einmal gesetzt — die zweite Abbuchung geht spurlos verloren. | Pro Socket eine In-Flight-Sperre (z.B. `room.betLocks`-Set) einführen, die parallele `place-bet`-Aufrufe desselben Sockets serialisiert/verwirft, bis die vorherige Transaktion abgeschlossen ist. |
| HIGH | server/handlers/maexchen.js:265-269 (analog 305-309) | `if (alive.length <= 1) { const winnerName = alive[0]?.name || 'Niemand'; await awardPotAndEndGame(io, room, winnerName, alive); return; }` | `room.game` wird erst ganz am Ende von `awardPotAndEndGame` (nach mehreren `await`s) auf `null` gesetzt. Feuert derselbe Client währenddessen erneut `challenge`/`believe-maexchen`, bestehen alle Guards weiterhin, wodurch `awardPotAndEndGame` ein zweites Mal aufgerufen wird und der Pot doppelt ausgezahlt wird. | Synchron VOR dem `await awardPotAndEndGame(...)` ein Guard-Flag setzen bzw. `room.game = null` markieren, damit nachfolgende `challenge`/`believe-maexchen`/`announce`-Events sofort per State-Check verworfen werden. |
| MEDIUM | server/handlers/maexchen.js:65-75 | `room.bets[socket.id] = amount;` | Trennt sich der Client (Disconnect) während der `await withWallet(...)`-Phase von `place-bet`, entfernt `removePlayerFromRoom` den Spieler bereits aus `room.players` und löscht `room.bets[socketId]`. Nach Abschluss des `await` schreibt der ursprüngliche `place-bet`-Aufruf `room.bets[socket.id] = amount` erneut — ein verwaister Eintrag für einen nicht mehr existierenden Spieler, dessen Coins bereits abgebucht wurden, aber nie in den Pot einfließen und auch nicht automatisch refundiert werden. | Nach dem `await` prüfen, ob der Spieler noch in `room.players` vorhanden ist; falls nicht, den abgebuchten Betrag automatisch zurückerstatten statt `room.bets` zu schreiben. |
| HIGH | server/handlers/pictochat.js:148-159 | `pictoState.inProgress.set(strokeId, stroke);` | `picto-stroke-segment` erlaubt beliebig viele verschiedene client-generierte `strokeId`s ohne globales oder Pro-Socket-Limit für gleichzeitig offene Strokes. Ein Client kann (innerhalb des Rate-Limits von 30/s) dauerhaft neue `strokeId`s senden, ohne je `picto-stroke-end` aufzurufen; Aufräumen erfolgt nur bei `stroke-end` oder Disconnect — solange die Verbindung offen bleibt, wächst die Map unbegrenzt (Memory-Exhaustion-DoS). | Harte Obergrenze für gleichzeitig offene Strokes pro Socket (z.B. 3-5) einführen und/oder ein TTL/Timeout für inaktive `inProgress`-Einträge. |
| MEDIUM | server/handlers/pictochat.js:294-303 | `pictoState.inProgress.clear();` | `picto-clear` löscht beim Aufruf durch EINEN Nutzer sämtliche `inProgress`-Strokes ALLER anderen gerade zeichnenden Nutzer, ohne diese zu benachrichtigen. Ruft ein anderer Nutzer danach `picto-stroke-end` für seinen (jetzt entfernten) Stroke auf, greift der `!stroke`-Guard und der Stroke geht clientseitig unbemerkt verloren. | Beim Clear nur `pictoState.strokes` leeren; für betroffene `inProgress`-Einträge gezielt ein `picto-stroke-cancel`-Event an die jeweiligen Autoren senden. |
| MEDIUM | server/handlers/pictochat.js:148-149,182-187 | `let stroke = pictoState.inProgress.get(strokeId);` | Die Eindeutigkeit von client-gelieferten `strokeId`s wird nur gegen `inProgress` geprüft, nicht gegen bereits committete Strokes in `pictoState.strokes`. Ein Client kann absichtlich eine `strokeId` senden, die mit einem bereits fertigen Stroke eines anderen Nutzers kollidiert; der zweite Stroke wird live angezeigt, aber wegen `on conflict (stroke_id) do nothing` nie persistiert (verschwindet nach Reload). | Beim Erzeugen eines neuen `inProgress`-Eintrags zusätzlich prüfen, ob `strokeId` bereits in `pictoState.strokes` vorkommt; bei Kollision Event verwerfen oder serverseitig neue ID vergeben. |
| LOW | server/handlers/pictochat.js:184 (i.V.m. server/pictochat-store.js:24) | `if (!stroke || stroke.authorId !== socket.id) return;` | Beim Laden aus der DB setzt `loadStrokes()` `authorId: null`. Nach Server-Neustart kann `picto-undo` (`s.authorId === socket.id`) diese Strokes nie finden, da `null !== socket.id` immer wahr ist — betroffene Nutzer können ihre eigenen, vor dem Neustart gezeichneten Strokes nicht mehr rückgängig machen. | `authorName` zusätzlich persistieren/laden und `picto-undo` alternativ per `authorName`-Abgleich zulassen, wenn `authorId` `null` ist. |
| LOW | server/handlers/plinko.js:54 (i.V.m. server/socket-handlers.js:82-88) | `if (!checkPlinkoCooldown(socket.id))` | Der Drop-Cooldown ist pro `socket.id`, nicht pro Spieler/Account gespeichert. Ein Nutzer kann den Cooldown durch zwei Tabs/Verbindungen mit demselben Login umgehen, da jede Verbindung ihren eigenen Cooldown-Timer hat (finanzielle Integrität bleibt durch `withWallet` gewahrt, aber das Drop-Pacing greift nicht). | Cooldown zusätzlich (oder statt `socket.id`) pro `player.name` tracken. |
| LOW | server/handlers/plinko.js:71-72 | `const risk = typeof data?.risk === 'string' ... ? data.risk : 'medium';` | Ein ungültiger/fehlender `risk`-Wert wird stillschweigend auf `'medium'` gesetzt, ohne den Client zu informieren — irreführend für Debugging und UI-Konsistenz. | Bei ungültigem `risk`-Wert einen expliziten `plinko-error` zurückgeben statt still auf `'medium'` zu defaulten. |
| MEDIUM | server/handlers/roulette.js:131,138-144 | `const spinCooldown = new Map(); // socketId -> timestamp` | Cooldown (1500ms) und Rate-Limit sind an `socket.id` statt an `player.name` gebunden. Ein Spieler kann über mehrere parallele Socket-Verbindungen (Multi-Tab) unabhängig voneinander spinnen — Spin-Taktung und Achievement-Zähler lassen sich so vervielfachen. | Cooldown/Rate-Limit zusätzlich (oder statt Socket-ID) pro `player.name` führen. |
| MEDIUM | server/handlers/roulette.js:161-231 | `const txResult = await withWallet(...) ... } catch (err) { ... socket.emit('roulette-error', { message: 'Spin failed.' }); }` | Sobald `withWallet` erfolgreich committet, laufen danach `bump()`, `notifyUnlocks`, `pushActivity` weiterhin im selben `try`-Block. Wirft eine dieser nachgelagerten Operationen eine Exception, greift der äußere `catch` und schickt nur `roulette-error`, obwohl der Kontostand serverseitig bereits real verändert wurde — Client zeigt veralteten/falschen Kontostand. | Committeten Spin-Ausgang sofort nach `withWallet` per `roulette-result`/`balance-update` senden, bevor nicht-kritische Nebenwirkungen (Achievements, Activity-Feed) in einem eigenen `try/catch` ausgeführt werden, der Fehler nur loggt. |
| LOW | server/handlers/soundboard.js:24 (i.V.m. server/socket-handlers.js:39-64) | `if (!checkRateLimit(socket, 3)) return;` | Der Rate-Limiter nutzt ein Fixed-Window-Verfahren (`resetTime = now + 1000`). Ein Client kann kurz vor Fensterende 3 Events senden und direkt danach (neues Fenster) erneut 3 senden — dadurch sind kurzzeitig bis zu ~6 `soundboard-play`-Events in deutlich unter 1 Sekunde möglich statt der beabsichtigten 3/s. | Sliding-Window- oder Token-Bucket-Algorithmus statt Fixed-Window verwenden. |
| HIGH | server/handlers/stocks.js:133,146,200,212 | `if (!checkStockTradeCooldown(socket.id)) {...} const result = await buyStock(player.name, quote.symbol, quote.price, amount);` | Der Trade-Cooldown wird über `socket.id`, nicht über `player.name` geführt. Ein Spieler mit zwei offenen Tabs (zwei Sockets, gleicher Name) kann die Serialisierung umgehen und zwei `stock-buy`/`stock-sell`-Events parallel auslösen; `buyStock`/`sellStock` aktualisieren `stock_positions` per SELECT-dann-UPDATE ohne `SELECT ... FOR UPDATE`, was zu einem Lost-Update bei Shares/AvgCost führt (Cash-Saldo bleibt über `deductBalance` korrekt). | Cooldown und Trade-Lock pro `player.name` (nicht `socket.id`) führen, z.B. In-Memory-Map `playerName -> Promise` zur seriellen Abarbeitung; zusätzlich `SELECT ... FOR UPDATE` auf `stock_positions` im DB-Pfad. |
| MEDIUM | server/handlers/stocks.js:152-154,218-220 | `emitToUser(io, player.name, 'balance-update', ...); ... socket.emit('stock-portfolio', snapshot);` | Nach einem Trade wird `balance-update` an ALLE Sockets des Spielers gebroadcastet, das neue `stock-portfolio` aber nur an den auslösenden Socket. Bei einer zweiten offenen Session/Tab zeigt diese danach einen aktualisierten Kontostand, aber ein veraltetes Portfolio, bis manuell neu angefordert wird. | `stock-portfolio`/`stock-portfolio-history` ebenfalls über `emitToUser` an alle Sockets des Spielers senden statt nur `socket.emit`. |
| LOW | server/handlers/stocks.js:122-123,189-190 | `data.symbol.replace(/[^A-Z0-9.\-=]/g, '').slice(0, 12)` | Die Sanitisierung lässt nur Großbuchstaben zu, ohne vorher zu normalisieren. Ein Client, der z.B. `"aapl"` sendet, verliert alle Kleinbuchstaben und landet als leerer String bei `INVALID_SYMBOL` — funktionale Fehlvalidierung, die legitime Ticker ablehnt. | Vor dem Regex `data.symbol.toUpperCase()` aufrufen. |
| LOW | server/handlers/stocks.js:24-30 | `const amount = Number(rawAmount); if (!Number.isFinite(amount) || !Number.isInteger(amount) || amount <= 0) { return null; }` | Keine Obergrenze für `amount`. `Number.isInteger()` liefert für Werte wie `1e21` `true`, sodass extrem große "Integer"-Werte die Validierung passieren. | Zusätzlich `amount > Number.MAX_SAFE_INTEGER` bzw. eine sinnvolle Business-Obergrenze prüfen und ablehnen. |
| HIGH | server/handlers/strictly7s.js:285-286,346-359 | `const fs = getFreeSpinState(player.name); ... fs.remaining -= 1;` | Zwei parallele Sockets desselben Spielers (Multi-Tab, gleicher `ownerToken`) haben getrennte Cooldowns (`checkStrictly7sCooldown` ist pro `socket.id`), teilen sich aber dasselbe `fs`-Objekt aus `freeSpinState` ohne Lock. Läuft z.B. `fs.remaining` bei Tab A auf 0 und wird `clearFreeSpinState` aufgerufen, während Tab B noch mit dem alten `fs`-Objekt arbeitet, dekrementiert Tab B `fs.remaining` auf -1 und liefert einen zusätzlichen kostenlosen Spin ohne `deductBalance`. | Free-Spin-Mutationen mit demselben `withBalanceLock`-Pattern wie bei Balance-Operationen pro `player.name` serialisieren; zusätzlich Cooldown auch pro Spielername (nicht nur `socket.id`) erzwingen. |
| MEDIUM | server/handlers/strictly7s.js:360-370 | `freeSpinState.set(player.name, newState);` | Lösen zwei parallele Spins desselben Spielers gleichzeitig einen Scatter-Trigger aus, berechnet jeder Handler unabhängig ein eigenes `newState`-Objekt; der zweite `set()`-Aufruf überschreibt den ersten vollständig (kein additives Merge) — eine der beiden gewonnenen Freispielrunden (10 Spins) geht stillschweigend verloren. | Beim erneuten Trigger prüfen, ob bereits ein `freeSpinState`-Eintrag existiert, und `remaining` addieren statt das Objekt zu ersetzen. |
| HIGH | server/handlers/tierlist.js:122-128 | `if (tierlistState.hydrated.has(weekKey)) return; const placements = await getAllPlacementsForWeek(weekKey); tierlistState.weekCache.set(weekKey, placements);` | `hydrateWeek` ist ein Check-then-Act ohne Sperre: Wird zwischen dem DB-Read und dem `weekCache.set(...)` ein `tierlist-place-item`-Event für dieselbe Woche verarbeitet, wird dessen bereits persistierte Platzierung durch den vollständigen Overwrite der Cache-Map wieder verworfen; da `hydrated` danach gesetzt bleibt, wird nie erneut hydriert — der Cache bleibt bis zum Neustart inkonsistent zur DB. | `hydrateWeek` mit einem Promise-Lock pro `weekKey` serialisieren und statt vollständigem Overwrite ein Merge in die bestehende Cache-Map durchführen. |
| HIGH | server/handlers/tierlist.js:203-244 (analog 246-281) | `await upsertPlacement(playerName, weekKey, itemIndex, tier); const weekData = tierlistState.weekCache.get(weekKey); ...` | Zwei schnell aufeinanderfolgende `tierlist-place-item`/`tierlist-remove-item`-Events desselben Spielers laufen als unabhängige async-Handler ohne Sequenzierung; DB-Write-Commit-Reihenfolge kann von der Cache-Update-/Broadcast-Reihenfolge abweichen, wodurch DB-Zustand und der an alle Clients verteilte Community-Zustand dauerhaft divergieren können. | Pro `(playerName, weekKey)` eine Request-Queue/Mutex einführen, die Platzierungs-/Entfernungs-Events serialisiert. **TEILWEISE VERWORFEN (Verifikation):** Die behauptete *dauerhafte* DB/Cache-Divergenz ist nicht belegt — die Cache-Mutation ist ein absoluter Set/Delete aus dem Event-Payload und läuft je Event STRIKT nach dem eigenen DB-Commit; DB, Cache und Broadcast konvergieren daher (last-write-wins) auf denselben Wert. Restrisiko: fehlende Sequenzierung kann die *Reihenfolge* zweier schneller Events invertieren (niedrig). Die echte Divergenz-Quelle ist `hydrateWeek` (Zeile 37), das bestätigt bleibt. |
| MEDIUM | server/handlers/tierlist.js:17-22,122-128 | `weekCache: new Map(), hydrated: new Set()` | `pruneOldWeeks` räumt nur die `memoryPlacements`-Map in `tierlist-store.js` auf, nicht aber `tierlistState.weekCache`/`hydrated` in diesem Handler — für jede neue ISO-Woche bleiben Cache-Einträge dauerhaft im Speicher (unbegrenztes Wachstum). | In `hydrateWeek` nach dem Hydrieren auch `weekCache`/`hydrated` auf die letzten N Wochen begrenzen. |
| LOW | server/handlers/tierlist.js:203-220,246-260 | `const player = onlinePlayers.get(socket.id); const playerName = player?.name || ''; if (!playerName) return;` | `tierlist-place-item`/`tierlist-remove-item` prüfen nicht, ob der Socket zuvor per `tierlist-join` beigetreten ist bzw. in `tierlistState.listeners` steht — der gebroadcastete `listeners`-Count wird dadurch inkonsistent mit den tatsächlich aktiven Rankern. | Vor dem Verarbeiten prüfen, ob `tierlistState.listeners.has(socket.id)` true ist, sonst früh zurückkehren. |
| HIGH | server/handlers/watchparty.js:99-103 | `socket.emit('watchparty-video', { videoId: room.watchparty.videoId, state: room.watchparty.state, time: room.watchparty.time });` | `watchparty-request-sync` (genutzt beim (Re-)Join/Reload) sendet weder `updatedAt` noch `serverTime`, obwohl Sync-Events laut Doku genau das tragen sollen. Läuft der Stream ("playing"), bekommt ein rejoinender Client den zum letzten `updatedAt` eingefrorenen `time`-Wert ohne Referenzzeitpunkt — der Client startet spürbar hinter der tatsächlichen Wiedergabeposition. | `watchparty-request-sync`-Payload um `updatedAt: room.watchparty.updatedAt, serverTime: Date.now()` ergänzen, analog zu `watchparty-sync`. |
| HIGH | server/handlers/watchparty.js:8,36,70 | `socket.on('watchparty-load', ...)` / `socket.on('watchparty-playpause', ...)` / `socket.on('watchparty-seek', ...)` | Keiner der drei State-ändernden Handler prüft, ob der sendende Client der Host/Owner des Rooms ist (`room.hostId` existiert im Datenmodell laut `server/room-manager.js`, wird hier aber nie referenziert). Jeder beliebige Mitspieler kann Video laden, pausieren/abspielen oder beliebig seeken und die Wiedergabe für alle anderen kapern/stören. | Vor der Zustandsänderung prüfen `if (socket.id !== room.hostId) { ...error...; return; }`, oder bewusst als Gruppenfunktion dokumentieren, falls gewollt. |
| MEDIUM | server/handlers/watchparty.js:5-115 | (gesamte Datei — kein `disconnect`-Handler) | Verlässt der einzige/letzte Nutzer den Room während `state === 'playing'`, bleibt `room.watchparty.state`/`time`/`updatedAt` unverändert stehen (kein Reset/Pause). Kommt später jemand zurück, wirkt der Player wegen des fehlenden `serverTime` (Finding oben) massiv nachlaufend/"eingefroren". | Bei `disconnect` prüfen, ob keine Mitspieler mehr im Room sind, und in diesem Fall `room.watchparty.state = 'paused'` setzen bzw. `updatedAt` aktualisieren. |
| LOW | server/handlers/watchparty.js:45,79 | `const time = typeof data.time === 'number' && isFinite(data.time) ? Math.max(0, data.time) : 0;` | `time` wird nur nach unten (≥0) begrenzt, es gibt keine Obergrenze — ein Client kann z.B. `time: 1e12` senden, das ungeprüft an alle anderen Clients per `watchparty-sync` verbreitet wird. | Obergrenze einführen (z.B. gegen bekannte Videolänge validieren oder eine sinnvolle Maximalgrenze). |
| HIGH | server/handlers/loop-machine.js:245-260,188 | `for (const instrument in loopState.grid) { const currentRow = loopState.grid[instrument] || []; if (currentRow.length > nextSteps) {...} }` | `loop-set-bars` und `loop-toggle-cell` teilen sich keinen Lock: `maxStep` wird in `loop-toggle-cell` gegen `loopState.bars` berechnet statt gegen die tatsächliche Array-Länge; bei schneller Eventfolge kann ein `loop-toggle-cell` mit stale `stepNum` nach einem `loop-set-bars`-Resize auf einen inzwischen kürzeren Array außerhalb der beabsichtigten Grenzen schreiben (Sparse-Array-Erweiterung), was den Grid-Row wieder verlängert und inkonsistent mit `bars` macht. | `loop-toggle-cell` sollte die tatsächliche Array-Länge `loopState.grid[instrument].length` statt der aus `bars` berechneten Grenze als Validierungsgrundlage verwenden. **VERWORFEN (Verifikation):** Zwischen Validierung (Z.185) und Write (Z.188) liegt kein `await`, und `loop-set-bars` resized alle Rows synchron — Array-Länge ist stets `bars*LOOP_STEPS_PER_BAR`, der Out-of-Bounds-/Sparse-Write ist nicht erreichbar. |
| MEDIUM | server/handlers/loop-machine.js:173-226 | `socket.on('loop-toggle-cell', async (data) => { ... loopState.grid[instrument][stepNum] = ...` | Client-Trust: Jeder verbundene Socket kann `loop-toggle-cell`, `loop-set-bpm`, `loop-clear` etc. senden und den global geteilten `loopState` für alle Zuhörer manipulieren, ohne dass geprüft wird, ob der Socket überhaupt im `LOOP_ROOM` ist bzw. `loop-join` aufgerufen hat. | Vor der Zustandsänderung prüfen, ob `loopState.listeners.has(socket.id)`, sonst Event ignorieren. |
| LOW | server/handlers/loop-machine.js:88-92 | `if (typeof saved.bpm === 'number') loopState.bpm = saved.bpm; if (typeof saved.bars === 'number') loopState.bars = saved.bars;` | `loadStateFromDB()` übernimmt `bpm`/`bars` aus der DB ohne die gleichen Grenzen zu prüfen, die die Socket-Handler erzwingen (60-200 bzw. 1-8). Bei einer manipulierten/alten DB-Zeile (z.B. `bars: 999`) berechnen nachfolgende `loop-toggle-cell`-Aufrufe riesige `maxStep`-Werte, die nicht mit der tatsächlichen Grid-Array-Länge übereinstimmen. | In `loadStateFromDB` dieselben Bounds-Checks wie in den Handlern anwenden und Grid-Zeilenlängen danach mit `bars` synchron abgleichen. |
| HIGH | server/routes/stocks.js:286-329,331-406,411-502 | `router.get('/api/stock-search', async (req, res) => {...}); router.get('/api/stock-quote', async (req, res) => {...}); router.get('/api/_stock-diag', async (req, res) => {... if (req.query.probe === '1') { ... await fetchQuotesViaStooq(testSyms); ... await fetchQuotesViaChart(testSyms); ... await getYahooFinance(); ... }});` | Keiner der vier Router-Endpunkte in `server/routes/stocks.js` hat ein Rate-Limit — anders als die socket-basierten Trades, die per `checkStockTradeCooldown` (`server/socket-handlers.js:66`) gedrosselt werden. `/api/stock-search` und `/api/stock-quote` haben zwar einen Cache (10 bzw. 2 Minuten), der aber pro Query-String/Symbol schlüsselt: ein Client kann den Cache trivial umgehen, indem er bei jedem Request eine andere Query sendet (z.B. `?q=AAPLX`, `?q=AAPLXX`, ...) oder verschiedene Symbole abfragt, und so bei jedem Request einen frischen Aufruf gegen Yahoo/Stooq erzwingen. Am gravierendsten ist `/api/_stock-diag?probe=1`: dieser Pfad hat GAR KEINEN Cache und feuert bei JEDEM Aufruf synchron vier echte Requests an externe Provider (Stooq-Batch, Yahoo-Spark, Yahoo-Chart, yf.quote) ab. Da alle drei Routen nur hinter der Session-Auth (gemeinsames `SITE_PASSWORD`) liegen, kann jeder eingeloggte Nutzer (oder ein Skript mit der Session-Cookie) diese Endpunkte in einer engen Schleife aufrufen und so Yahoo/Stooq mit beliebig hoher Frequenz von der Server-IP aus anfragen — Risiko eines IP-Bans/Rate-Limits bei den externen Providern, der dann den Stock-Ticker für ALLE Spieler ausfallen lässt. | Ein einfaches Request-Rate-Limit (pro Session/IP, analog zum Login-Limiter in `auth.js`) auf `/api/stock-search`, `/api/stock-quote` und insbesondere `/api/_stock-diag` (idealerweise `probe=1` ganz hinter ein Admin-Token statt nur Session-Auth stellen, da es aktiv teure externe Calls auslöst) einführen. |
| LOW | server/routes/stocks.js:384-391 | `const errors = []; for (const fn of tryProviders) { try { data = await fn(); break; } catch (e) { errors.push(e.message); } } if (!data) { return res.status(502).json({ error: 'all providers failed: ' + errors.join(' | ') }); }` | Bei einem Fehlschlag aller drei Provider werden deren rohe `.message`-Strings (können interne Fehlerdetails wie URLs, Hostnamen oder Stack-Infos aus `yahoo-finance2`/Stooq-Fetch-Fehlern enthalten) direkt an den Client durchgereicht. Kein sensibler Secret-Leak, aber unnötige Offenlegung interner Implementierungsdetails an jeden eingeloggten Nutzer. | Nur eine generische Fehlermeldung an den Client senden (`'Failed to fetch quote'`) und die Provider-Fehlermeldungen ausschließlich server-seitig loggen (analog zum bereits vorhandenen `console.error` in `/api/ticker`). |
| LOW | server/routes/auth.js:6,34-43 | `const loginRateLimiter = new Map(); // ip -> { count, resetAt } ... const entry = loginRateLimiter.get(ip); if (entry && now < entry.resetAt) { entry.count += 1; ... } else { loginRateLimiter.set(ip, { count: 1, resetAt: now + LOGIN_RATE_LIMIT_WINDOW_MS }); }` | Im Gegensatz zu den Socket-Rate-Limitern in `server/socket-handlers.js` (dort räumt ein periodischer Sweep `rateLimiters`/`rateLimitersIp` abgelaufene Einträge auf, Z.92-96) gibt es für `loginRateLimiter` KEINEN Cleanup-Mechanismus. Ein Eintrag wird nur bei erfolgreichem Login per `loginRateLimiter.delete(ip)` entfernt (Z.54) — bei falschem Passwort bleibt er dauerhaft im Speicher, auch nachdem `resetAt` längst verstrichen ist. Da `req.ip` bei `trust proxy=1` aus `X-Forwarded-For` gelesen wird, kann ein Angreifer mit gefälschten/rotierenden Absenderadressen (oder einfach viele unterschiedliche echte Clients mit falschem Passwort) den Map unbegrenzt wachsen lassen — langsamer Memory-Leak/DoS-Vektor auf einem Endpunkt, der explizit ohne Session-Auth erreichbar ist (`/login` liegt vor `authMiddleware`). | Periodischen Sweep (analog zu `server/socket-handlers.js:92-96`) ergänzen, der abgelaufene `loginRateLimiter`-Einträge (`now > entry.resetAt`) entfernt, z.B. beim bereits vorhandenen periodischen Cleanup-Intervall in `server/cleanup.js` mit registrieren. |
| MEDIUM | games/crash/js/game.js:150-159 | `const dt = performance.now() - lastTick.atTime; const projected = lastTick.elapsedMs + dt; const m = Math.exp(0.08 * (projected / 1000)); displayMultiplier = m;` | Client extrapoliert den laufenden Multiplikator lokal mit einer fest angenommenen Wachstumsformel (`0.08`), statt ausschließlich auf `crash-tick`-Events zu warten. Weicht die tatsächliche Server-Kurve (Jitter, andere Konstante, Netzwerklatenz) ab, zeigt der Client kurzzeitig einen falschen Multiplikator an, auf dessen Basis der Spieler die Cashout-Entscheidung trifft, obwohl der reale Payout beim Cashout vom Server (`crash-cashout-confirmed`) bestimmt wird. | Anzeige stärker an die zuletzt empfangenen `crash-tick`-Daten koppeln (kürzeres Interpolationsfenster) oder zusätzlich serverseitige Sync-Toleranz/Korrektur-Hinweis einblenden, damit Nutzer nicht von der Anzeige getäuscht werden, auch wenn der Payout korrekt bleibt. |
| LOW | games/blackjack/js/game.js:170-178 | `const playerBJ = state.playerHand?.length === 2 && pTotal?.total === 21; ... const dealerBJ = state.finished && state.dealerHand?.length === 2 && dTotal?.total === 21;` | Client leitet die "BJ!"-Anzeige (Total-Badge) selbst aus Handlänge+Total ab, statt ein explizites Server-Flag zu verwenden. Bei künftigen Regeländerungen kann die Badge-Anzeige vom tatsächlichen `state.outcome` des Servers abweichen, auch wenn der Gewinn-Banner selbst korrekt `state.outcome` nutzt. | Server sollte ein explizites `isBlackjack`-Flag pro Hand mitsenden, das der Client 1:1 übernimmt, statt es erneut zu berechnen. |
| LOW | games/casino/js/casino.js:30 | `document.querySelectorAll('.casino-card.coming-soon').forEach(card => {` | Totes Code-Handling: In `games/casino/index.html` hat keine `.casino-card` mehr die Klasse `coming-soon` (alle sechs Kacheln sind `data-status="live"`), der Listener greift also nie. Kein Sicherheitsproblem, aber Hinweis auf veralteten/inkonsistenten Code. | Toten Code entfernen oder auf `[data-status="soon"]` (das tatsächlich verwendete Attribut, siehe casino.css:166) umstellen. |
| HIGH | games/maexchen/js/game.js:695,702 | `` `<img src="${char.dataURL}" class="pixel-avatar-img" alt="${p.name}">` `` und `` `<div class="sidebar-name">${p.name}</div>` `` | `renderPlayerSidebar` rendert `p.name` ungefiltert per `innerHTML`, obwohl andere Stellen derselben Datei (z.B. Zeile 495, 588) dieselbe Quelle konsequent mit `escapeHtml()` behandeln. Ein Spielername mit HTML/JS-Payload führt zu Stored-XSS gegenüber allen Mitspielern in der Sidebar. | `escapeHtml(p.name)` verwenden wie an den anderen Renderstellen; `char.dataURL` zusätzlich auf `data:image/...`-Prefix validieren, bevor es ins Attribut interpoliert wird. **TEILWEISE VERWORFEN (Verifikation):** Der `p.name`-XSS ist verworfen — `sanitizeName` (socket-utils.js:1-6) strippt `<>&"'/`, ein Name kann weder Attribut noch Textknoten aufbrechen (nur Hygiene-Inkonsistenz). Der `char.dataURL`-XSS ist BESTÄTIGT (identisch zu Zeile 55). |
| HIGH | games/maexchen/js/game.js:695 | `` `<img src="${char.dataURL}" ...>` `` | `char.dataURL` wird ohne Escaping/Validierung direkt ins `src`-Attribut eines via `innerHTML` gesetzten Strings eingebettet — ein Wert wie `"><script>...` bricht das Attribut aus (HTML-Injection). | `escapeHtml()` anwenden oder das Bild über `document.createElement('img')` + `img.src = char.dataURL` (DOM-API statt String-Konkatenation) setzen. |
| HIGH | games/maexchen/js/game.js:243-248 | `` overlay.innerHTML = `<div class="player-name">${playerName} sagt:</div><div class="announced-value ${isMax ? 'maexchen' : ''}">${valueName}</div>` `` | In `showBigAnnouncement` werden `playerName`/`valueName` ungeschützt per `innerHTML` an alle Clients gerendert → Stored-XSS, sichtbar für die gesamte Lobby als großes Overlay. | `escapeHtml()` auf `playerName`/`valueName` anwenden, wie bereits an Zeile 495 für dieselbe Datenquelle. **VERWORFEN (Verifikation):** `playerName` stammt aus dem server-sanitisierten Namen und `valueName` aus dem serverseitigen Enum `rollName()` (nur `'Mäxchen!'`/`Pasch N`/Zahl) — beide sind nicht angreiferkontrolliert, kein exploitierbarer XSS (nur Hygiene-Inkonsistenz). |
| HIGH | games/maexchen/js/game.js:121-124 | `` list.innerHTML = bets.filter(b => b.bet > 0).map(b => `<div class="bet-entry"><span>${b.name}</span>...`).join(''); `` | Spielername `b.name` aus dem `bets-update`-Event wird ungeschützt per `innerHTML` gerendert → Stored-XSS in der Wartezimmer-Wettliste (echter Geldeinsatz-Kontext). | `escapeHtml(b.name)` verwenden, konsistent mit dem Rest der Datei. **VERWORFEN (Verifikation):** `b.name` durchläuft serverseitig `sanitizeName` (strippt `<>&"'/`), ein Breakout aus `<span>${b.name}</span>` ist nicht möglich — kein exploitierbarer XSS, nur Hygiene-Inkonsistenz. |
| MEDIUM | games/maexchen/js/game.js:129-130,292-296 | `state.myPlayerIndex = players.findIndex(p => p.name === state.playerName);` (nur bei `game-started` gesetzt) | `myPlayerIndex` wird nur einmal bei Spielstart berechnet und nicht neu ermittelt, obwohl `state.gamePlayers` bei `turn-start`/`player-disconnected` neu zugewiesen wird. Bei geänderter Spielerreihenfolge/-länge kann die "bin ich dran"-Anzeige (`isMyTurn`) falsch sein. | `myPlayerIndex` bei jedem `players`-Update neu berechnen statt nur bei `game-started`. |
| LOW | games/maexchen/js/game.js:580,588 | `window.MaexchenAvatars.getAvatar(p.name)` Ergebnis ungeschützt in `innerHTML` interpoliert | `avatars.js` liegt außerhalb des Scopes; falls `getAvatar` Rohtext statt garantiertem Emoji/Markup liefert, wäre dies eine weitere ungeschützte `innerHTML`-Quelle (unbestätigt). | avatars.js prüfen; sicherheitshalber Rückgabewert escapen oder als reines Emoji/Icon-Enum garantieren. |
| HIGH | games/food-guessr/js/game.js:450 | `socket.emit('food-classic-finish', { score: totalScore, perfect: pct === 100 });` | Der komplette Classic-Score wird rein client-seitig akkumuliert und ungeprüft ans Leaderboard gesendet. Ein manipulierter Client kann `totalScore`/`perfect` beliebig fälschen und sich an die Spitze des Leaderboards setzen. | Server soll Runden-Ergebnisse (Ziel-Land, verbleibende Leben je Runde) selbst kennen/validieren und den Score serverseitig berechnen statt den Client-Wert zu übernehmen. **VERWORFEN wie formuliert (Verifikation):** Der Server ist nicht wehrlos — `server/handlers/food-guessr.js:121-122` clamped `score` auf `[0, 3000]` (`MAX_CLASSIC_SCORE`) und koppelt `perfect` an `score>=3000`; zudem fließt der Wert nur ins Leaderboard, KEINE Coins/Diamanten. Restrisiko: gedeckelter, nicht-monetärer Leaderboard-Spoof (niedrig), kein beliebiger Score. |
| MEDIUM | games/food-guessr/js/game.js:336-338 | `var earned = SCORE_BY_LIVES[lives] \|\| 0; totalScore += earned; roundScores.push({ dish: currentDish, guessed: guess.name, livesLeft: lives, earned: earned, correct: true });` | Die Punkteberechnung pro Runde (spielentscheidender Wert) läuft komplett im Client über eine lokale Tabelle `SCORE_BY_LIVES` und ist Grundlage für den oben genannten Fund. | Punkteberechnung serverseitig durchführen; Client sendet nur den Guess, Server antwortet mit autoritativem Score/verbleibenden Leben. |
| HIGH | games/food-guessr/js/game.js:801 | `socket.emit('food-scrandle-finish', { variant: variant, streak: streak });` | Die Scrandle-Streak wird vollständig client-seitig gezählt und am Ende ungeprüft an den Server fürs Leaderboard übertragen. Ein manipulierter Client kann eine beliebig hohe Streak faken. | Server soll jede Higher/Lower-Entscheidung serverseitig bestätigen (Round-Token je Vergleichspaar) statt der finalen Client-Streak blind zu vertrauen. **VERWORFEN wie formuliert (Verifikation):** `server/handlers/food-guessr.js:141-147` validiert `variant` gegen `'wiki'|'community'` und clamped `streak` auf `[0, 10000]`; der Wert landet nur im Leaderboard-Store, KEINE Coins. Restrisiko: gedeckelter, nicht-monetärer Leaderboard-Spoof (niedrig). |
| MEDIUM | games/food-guessr/js/game.js:719-726 | `var correct; if (direction === 'higher') correct = rightValue >= leftValue; else correct = rightValue <= leftValue;` | Richtig/Falsch-Entscheidung für Scrandle (Basis der Streak) wird im Client getroffen, teils mit Werten aus direktem Client-Fetch der externen Wikipedia-API (`window.FG_fetchDishViews`), ohne serverseitige Verifikation. | Wikipedia-Pageviews serverseitig cachen/liefern (autoritative Quelle), oder die eingereichte Streak serverseitig gegen bekannte Werte plausibilisieren. |
| LOW | games/food-guessr/js/game.js:188-200 | `function emitVote(dishKey, rating) { if (!socket \|\| !socket.connected) { myVotes[dishKey] = rating; ... } socket.emit('food-rating-vote', ...); }` | Bei fehlender Socket-Verbindung wird die Community-Bewertung rein lokal simuliert (optimistic update), aber nie persistiert — Nutzer sieht scheinbar erfolgreiche Abstimmung, die verloren geht. | UI-Hinweis "nicht gespeichert" statt stillschweigendem lokalem Fake-Update. |
| HIGH | games/strictbrain/js/game.js:264-272 | `socket.emit('brain-submit-score', { playerName: playerName, brainAge: brainAge, coins: coins, games: dailyResults.map(r => ({ gameId: r.gameId, score: r.score })) });` | `brainAge` und `coins` werden vollständig im Client via `calculateBrainAge`/`calculateCoins` berechnet und dann direkt als Auszahlungs-/Ergebniswert an den Server geschickt. | Server soll aus den rohen Spielereignissen selbst `brainAge`/`coins` berechnen; Client-Felder ignorieren oder nur als Hinweis behandeln. **VERWORFEN wie formuliert (Verifikation):** Der Server ignoriert das Client-`coins`-Feld und rechnet `coins = calculateBrainCoins(brainAge)` selbst (`server/handlers/brain-versus.js:137`, Kommentar "don't trust client"); `brainAge` wird auf 20–80 range-validiert und der Claim ist auf 1×/UTC-Tag begrenzt. Restrisiko: `brainAge` ist client-geliefert und bestimmt die Coin-Stufe, aber gedeckelt auf 5–50 SC/Tag (niedrig). |
| HIGH | games/strictbrain/js/game.js:313-319 | `socket.emit('brain-training-score', { playerName: playerName, coins: coins, gameId: gameId, score: score });` | Auch im freien Training wird die Coin-Belohnung (`coins`, aus `calculateCoins`) client-seitig berechnet und zur Auszahlung übermittelt. | Server berechnet Coins serverseitig aus `gameId`/`score`; `coins`-Feld vom Client nicht vertrauen. **VERWORFEN wie formuliert (Verifikation):** Der Server tut genau das bereits — `server/handlers/brain-versus.js:224` berechnet `coins = calculateTrainingCoins(coinScore)` aus dem validierten `score` und ignoriert das Client-`coins`-Feld; zusätzlich 20s-Cooldown pro Name und 200 SC/Tag-Cap. Restrisiko: `score` ist client-geliefert (gedeckelt), niedrig. |
| HIGH | games/strictbrain/js/game.js:1148-1153 | `function versusScoreUpdate(rawScore) { versusMyScore = rawScore; ... socket.emit('brain-versus-score-update', { score: rawScore }); }` | Alle Versus-Minispiele werten Antworten vollständig im Client aus; das Ergebnis wird 1:1 an den Server gemeldet. Über die DevTools-Konsole kann `socket.emit('brain-versus-score-update', {score: 99999})` beliebig ausgeführt werden, um garantiert zu gewinnen. | Server muss Punktestände unabhängig verifizieren (z.B. Aufgaben/Antworten serverseitig prüfen) oder zumindest Update-Frequenz/Score-Sprünge plausibilisieren und begrenzen. **TEILWEISE VERWORFEN (Verifikation):** `brain-versus-score-update` (`brain-versus.js:327-329`) speist NUR das Live-Scoreboard-Display (auf 9999 geclamped) und entscheidet NICHT über Sieg/Auszahlung — "garantiert gewinnen" ist hierüber nicht möglich. Der reale Payout-Cheat sitzt in `brain-versus-finished` (Zeile 68 dieser Tabelle), das bleibt bestätigt HIGH. |
| HIGH | games/strictbrain/js/game.js:1155-1158 | `function versusFinish(normalizedScore) { clearInterval(versusTimer); socket.emit('brain-versus-finished', { score: normalizedScore }); }` | Der finale Score, der laut `brain-versus-result`-Handler direkt über Sieg/Niederlage und Coin-Auszahlung entscheidet, wird ebenfalls nur client-berechnet gemeldet. | Wie oben: serverseitige Score-Verifikation vor Sieger-Feststellung/Auszahlung. |
| MEDIUM | games/strictbrain/js/game.js:1100-1101 | `const myCoins = data.isDraw ? data.coins : (data.winner === playerName ? data.coins : 5); $('versus-results-coins-amount').textContent = '+' + myCoins + ' StrictCoins';` | Bei Niederlage wird der Trostpreis hart auf `5` Coins im Client geraten statt vom Server übernommen; weicht der tatsächlich gutgeschriebene Betrag davon ab, zeigt die UI ein falsches Ergebnis an. | Server soll den tatsächlichen Auszahlungsbetrag jedes Spielers im `brain-versus-result`-Payload mitschicken (z.B. `data.myCoins`) statt ihn clientseitig zu erraten. |
| LOW | games/strictbrain/js/game.js:1011-1017 | `$('versus-professor-text').textContent = escapeHtml(data.playerName) + ' hat den Raum verlassen.';` | `escapeHtml()`-Output wird auf `.textContent` gesetzt statt auf `.innerHTML`; da `.textContent` HTML-Entities nicht dekodiert, wird ein Name mit `&`, `<` etc. verstümmelt angezeigt. Kein XSS, aber ein Anzeige-Bug durch vermischte Escaping-Strategie. | `.textContent = data.playerName + ' hat den Raum verlassen.'` (ohne escapeHtml) oder auf `innerHTML` mit escapeHtml umstellen. |
| LOW | games/strictbrain/js/game.js:985-989 | `setInterval(() => { if ($('screen-versus-lobby')?.classList.contains('active') && !versusRoomCode) { requestVersusLobbies(); } }, 5000);` | Page-lebenslanges `setInterval`, das nie geklärt wird (kein `clearInterval`); läuft für die gesamte Tab-Lebensdauer weiter, unabhängig davon wie lange der Spieler schon in einem Match ist. | Intervall-ID speichern und beim Verlassen der Strict-Brain-Seite (`beforeunload`) oder beim Wechsel aus dem Versus-Lobby-Screen per `clearInterval` stoppen. |
| MEDIUM | games/tierlist/js/game.js:254-265,406 | `function setupDropZones() { var zones = document.querySelectorAll('.tier-items'); zones.forEach(function (zone) { zone.addEventListener('dragover', onDragOver); ... }); }` aufgerufen in `socket.on('tierlist-sync', ...)` | `setupDropZones()` wird bei jedem `tierlist-sync` (Join + jeder Reconnect) erneut auf denselben, nie neu erzeugten DOM-Zonen aufgerufen. `renderMyView()` leert nur `innerHTML` der Zonen, die Zonen-Elemente selbst bleiben bestehen → dragover/dragleave/drop-Listener akkumulieren sich bei jedem Reconnect, wodurch `onDrop` mehrfach pro Drop feuert und `placeItem`/`removeItem` mehrfach ausgelöst wird. | Listener nur einmal beim initialen Laden registrieren (außerhalb des `tierlist-sync`-Handlers), oder vor erneutem Binden `zone.replaceWith(zone.cloneNode(true))` bzw. ein Flag `dropZonesInitialized` prüfen. |
| MEDIUM | games/tierlist/js/game.js:358-380 | `myPlacements[itemIndex] = tier; renderMyView(); socket.emit('tierlist-place-item', ...)` / `delete myPlacements[itemIndex]; ...; socket.emit('tierlist-remove-item', ...)` | Optimistisches Update wird sofort übernommen, aber es gibt keinen Error-Listener im gesamten File. Lehnt der Server eine Platzierung ab (Rate-Limit, ungültiger Index/Tier), bleibt der Client dauerhaft in einem falschen State bis zum nächsten vollen `tierlist-sync`. | Serverseitiges Error-Event ergänzen und im Client abfangen, um das optimistische Update zurückzurollen. |
| LOW | games/tierlist/js/game.js:158,171 | `html += '<div class="community-avg-badge avg-' + data.avgTier + '">' + data.avgTier + '</div>';` und `html += '<div class="bar-segment" style="width:' + pct + '%;background:' + TIER_COLORS[tier] + '" title="' + tier + ': ' + data[tier] + '"></div>';` | `data.avgTier`/`data[tier]` (Server-Payload `communityData`) werden ungeprüft und unescaped in `innerHTML`-String-Konkatenation eingesetzt (Klassenname und Attributwert), anders als bei Item-Namen (dort konsequent `escapeHtml`). Sollte der Server-Payload je kompromittiert/fehlerhaft sein, ist Attribut-/Class-Injection möglich. | `escapeHtml()` auch auf `data.avgTier` und `data[tier]` anwenden, bzw. `avgTier` zusätzlich gegen die feste `TIERS`-Whitelist validieren. |
| LOW | games/watchparty/js/watchparty.js:57-85,210-216 | `function createPlayer(videoId) { ... container.innerHTML = '<div id="yt-player"></div>'; loadYouTubeAPI().then(function () { ytPlayer = new YT.Player(...) }); }` aufgerufen aus `socket.on('watchparty-video', ...)` | Beim Laden eines neuen Videos wird die alte `ytPlayer`-Instanz nie mit `.destroy()` freigegeben, bevor sie überschrieben wird. Über mehrere Videowechsel in einer Session akkumulieren sich verwaiste YouTube-IFrame-API-Objekte/Message-Listener. | Vor dem Neuerstellen `if (ytPlayer && ytPlayer.destroy) { ytPlayer.destroy(); ytPlayer = null; }` aufrufen, analog zur bestehenden `cleanup()`-Logik. |
| LOW | games/watchparty/js/watchparty.js:94-98,104-110 | `if (playerState === YT.PlayerState.PLAYING) { socket.emit('watchparty-playpause', { state: 'playing', time: time }); }` / `socket.emit('watchparty-seek', time);` | Der Client liefert die Wiedergabezeit selbst und der Server broadcastet sie ungeprüft an alle Zuschauer; auch wenn kein Payout-relevanter Wert, sollte der Server `time`/`state` plausibilisieren, damit kein Client beliebige/negative Werte an alle broadcasten kann. | Serverseitige Validierung von `time`/`state` ergänzen (Typ, Bereich, Rate-Limit pro Socket). |
| LOW | games/plinko/js/game.js:173,181 | `mult.className = 'history-mult ' + (h.payout > h.bet ? 'win' : 'loss');` (173) vs. `pay.className = 'history-pay ' + (h.payout >= h.bet ? 'win' : 'loss');` (181) | Inkonsistente Schwellenwerte (`>` vs. `>=`) für denselben Drop: bei Break-even wird der Multiplikator rot ("loss"), der Payout aber grün ("win", zeigt "+0") angezeigt — widersprüchliche Gewinn-/Verlustanzeige im Verlaufspanel. | Beide Vergleiche auf denselben Operator (`>=`, konsistent mit `handleResult`, Zeile 385) vereinheitlichen. |
| LOW | games/plinko/js/game.js:117 | `const text = m >= 1 ? \`${m}×\` : \`${m}×\`;` | Toter Code: beide Zweige der Ternary sind identisch, die Bedingung hat keinerlei Effekt. | Ternary entfernen (`const text = \`${m}×\`;`) oder tatsächlich unterschiedliche Formatierung für Sub-1x-Multiplikatoren implementieren, falls beabsichtigt. |
| MEDIUM | games/roulette/js/game.js:374-378 | `const rows = betListEl.querySelectorAll('.bet-row'); result.results.forEach((r, i) => { if (rows[i]) rows[i].classList.add(r.won ? 'win' : 'loss'); });` | Zuordnung von Server-Ergebnis zu Bet-Zeile erfolgt rein über Array-Index, ohne Bet-Identität (Typ/Wert) zu prüfen. Weicht die Reihenfolge von `result.results` (Server) von der Reihenfolge in `activeBets` (Client) ab, wird die falsche Wette als Gewinn/Verlust markiert. Der `net`/`balance`-Wert bleibt korrekt, aber die visuelle Win/Loss-Markierung pro Einzelwette kann irreführend falsch sein. | `result.results` mit `{type, value, won, amount}` statt reinem Boolean-Array zurückgeben lassen (serverseitig) und clientseitig anhand `type`/`value` statt Index matchen. |
| LOW | games/roulette/js/game.js:155 | `row.innerHTML = \`<span>${label}</span><span>${b.amount} SC</span>\`;` | Nutzung von `innerHTML` mit String-Interpolation statt `textContent`. Aktuell ungefährlich, da `label`/`b.amount` nur aus festen Enum-Werten bzw. Zahlen stammen, aber unsauberes Pattern, das bei künftiger Erweiterung zu einer XSS-Lücke werden kann. | Auf `textContent`/`createElement` umstellen oder die in stocks/js/game.js vorhandene `escapeHtml`-Hilfsfunktion mitverwenden. |
| MEDIUM | shared/js/creator.js:125,140-142 | `function setupEventListeners(creator, grid, palette) { ... document.addEventListener('mouseup', () => { isDrawing = false; }); ... }` | `setupEventListeners()` wird bei JEDEM Aufruf von `createCreatorUI()` (also bei jedem `showCreator()`, z.B. Klick auf "Charakter ändern") neu ausgeführt und registriert dabei einen neuen anonymen `mouseup`-Listener direkt auf `document` — ohne jemals den vorherigen zu entfernen (keine Referenz wird gespeichert, kein `removeEventListener`). Auch `grid`/`palette`-Listener werden neu gebunden, aber `grid`/`palette` sind Teil des bei jedem Aufruf neu erzeugten `#character-creator`-DOM (wird via `existing.remove()` entfernt und ersetzt), daher verschwinden diese mit dem alten Element. Der `document`-Listener bleibt jedoch dauerhaft bestehen. In einer langlebigen Lobby-Session, in der ein Spieler den Charakter-Editor mehrfach öffnet (z.B. 10×), sammeln sich 10 identische `mouseup`-Listener auf `document` an — klassischer Memory Leak, der mit jeder erneuten Öffnung wächst und bei jedem `mouseup`-Event unnötig mehrfach `isDrawing = false` setzt. | Den `mouseup`-Handler wie in `shared/js/chat.js` (`documentMouseUpHandler`) in einer Modul-Variable speichern, vor dem erneuten Registrieren mit `document.removeEventListener` entfernen (oder nur einmalig außerhalb von `createCreatorUI()`/`setupEventListeners()` registrieren, analog zum `noteGridInitialized`-Guard in chat.js). |
| MEDIUM | server/routes/auth.js:8 | `const PASSWORD = (process.env.SITE_PASSWORD || 'ADMIN').toLowerCase();` | Ist `SITE_PASSWORD` in der Deployment-Umgebung nicht gesetzt (Fehlkonfiguration, vergessene Env-Var bei einem neuen Render-Deploy etc.), fällt das gesamte Site-Login auf das hartcodierte Default-Passwort `"ADMIN"` zurück — zusätzlich case-insensitiv, da sowohl der eingegebene Wert als auch `PASSWORD` per `.toLowerCase()` normalisiert werden (`"admin"`, `"Admin"`, `"ADMIN"` funktionieren alle). Das Passwort ist zudem in `README.md`/`HANDOFF.md` öffentlich dokumentiert. Da `SESSION_SECRET` bei fehlendem Env explizit in Produktion hart abbricht (Z.50-52 in `server/index.js`), fehlt hier ein analoger Fail-Fast-Schutz — ein falsch konfiguriertes Prod-Deployment bleibt mit einem trivialen Passwort erreichbar, statt beim Start sichtbar zu scheitern. | Wie bei `SESSION_SECRET`: in Produktion (`NODE_ENV === 'production'`) den Prozess abbrechen, wenn `SITE_PASSWORD` nicht gesetzt ist, statt auf `'ADMIN'` zurückzufallen. |

## Werkzeuggestützter Prüf-Pass (npm test, Lint, Grep-Sweeps, npm audit)

| HIGH | server/game-logic.js:27-33 | `export function rollDice() { const a = Math.floor(Math.random() * 6) + 1; const b = Math.floor(Math.random() * 6) + 1; ... }` | `rollDice()` erzeugt die Würfelwerte für Mäxchen (aufgerufen in `server/handlers/maexchen.js:163`, `game.currentRoll = rollDice();`) — ein Spiel mit echtem Währungseinsatz — mit dem unsicheren `Math.random()`. Alle anderen Glücksspiel-RNGs derselben Codebasis (`server/handlers/blackjack.js:1,51`, `roulette.js:1,35`, `plinko.js:1,36`, `strictly7s.js:1,96`, `crash.js:1,31-35`) verwenden konsequent `crypto.randomInt`, gerade weil `Math.random()` ein nicht-kryptografischer PRNG ist, dessen interner Zustand aus einer Ausgabesequenz rekonstruierbar ist. `rollDice()` ist die einzige Ausnahme von dieser sonst durchgängig durchgesetzten Konvention. | `rollDice()` analog zu den anderen Handlern auf `crypto.randomInt` umstellen, z.B. `import { randomInt } from 'crypto'; const a = randomInt(1, 7); const b = randomInt(1, 7);`. |
| HIGH | shared/js/chat.js:357-360 | `` msgEl.innerHTML = `<div class="sender">${escapeHtml(sender)} ${targetText}</div><img src="${dataURL}" class="chat-drawing" alt="Zeichnung">`; `` | `dataURL` aus dem `drawing-note`-Event wird ungeschützt in das `src`-Attribut interpoliert, während `sender` direkt daneben korrekt per `escapeHtml()` behandelt wird. Serverseitig prüft `server/handlers/lobby.js:212` nur `dataURL.startsWith('data:image/')` — ein manipulierter Client kann z.B. `data:image/png" onerror="alert(document.cookie)` senden, das den Prefix-Check besteht, aber das `src`-Attribut aufbricht und ein `onerror`-Attribut injiziert (Stored-XSS gegenüber allen Empfängern der Zeichnung im Chat). | `dataURL` vor der Interpolation escapen (z.B. mit einer quote-sicheren `escapeAttr()`-Funktion wie in `games/stocks/js/game.js`) oder das `<img>` per `document.createElement('img')` + `img.src = dataURL` statt String-Konkatenation setzen; zusätzlich serverseitig strikter validieren (z.B. Regex auf gültiges Base64-Encoding statt nur Prefix-Check). |
| HIGH | games/watchparty/js/watchparty.js:308-318 | `` var avatarHtml = p.character && p.character.dataURL ? '<img src="' + p.character.dataURL + '" alt="" ...>' : '👽'; ... '<span>' + escapeHtml(p.name) + '</span>' `` | `renderPartyPlayers` interpoliert `p.character.dataURL` (aus `room-update`, vom jeweiligen Spieler selbst gesetzt) roh in ein `src`-Attribut, während direkt darunter `p.name` korrekt mit `escapeHtml()` behandelt wird. Die serverseitige `validateCharacter()` (`server/socket-utils.js:20`) prüft ebenfalls nur den `data:image/`-Prefix, sodass ein Payload wie `data:image/png" onerror="alert(1)` das Attribut aufbrechen kann — Stored-XSS gegenüber allen Mitspielern in der Watchparty-Spielerleiste. Analog zum bereits dokumentierten Fund in `games/maexchen/js/game.js:695`, aber an anderer Stelle im Code. | `escapeHtml()`/`escapeAttr()` auf `p.character.dataURL` anwenden (konsistent mit `p.name` in derselben Funktion) oder das `<img>` per DOM-API (`img.src = ...`) statt String-Konkatenation setzen. |
| MEDIUM | shared/js/emotes.js:127-133 | `` popup.innerHTML = `<div class="emote-popup-emoji">${emote.emoji}</div><div class="emote-popup-info"><div class="emote-popup-player">${avatarHTML} ${playerName}</div>...`; `` | `playerName` aus dem `emote-broadcast`-Event wird in `showEmotePopup` ohne Escaping in `innerHTML` gerendert — die Datei definiert keine eigene `escapeHtml()`-Hilfsfunktion. `player.name` durchläuft zwar serverseitig `sanitizeName()` (`server/socket-utils.js:1-6`, entfernt `<>&"'/`), doch ist dies reine serverseitige Deny-List-Filterung ohne clientseitiges Escaping — anders als praktisch überall sonst in der Codebase (chat.js, lobby.js, tierlist.js, maexchen/game.js), wo dieselbe Namensquelle zusätzlich konsequent escaped wird. Ändert sich `sanitizeName` künftig oder gibt es einen Umgehungspfad, wird dies sofort zu Stored-XSS. | `escapeHtml(playerName)` einführen (Helper existiert bereits an vielen anderen Stellen im Projekt, z.B. `shared/js/chat.js`) und konsequent auf `playerName` anwenden, statt sich allein auf serverseitige Sanitisierung zu verlassen. |
| MEDIUM | server/stock-providers/yahoo-chart.js:122-127 | `for (let i = 0; i < symbols.length; i += SPARK_BATCH_SIZE) { const chunk = symbols.slice(i, i + SPARK_BATCH_SIZE); const { quotes, errors } = await fetchSparkBatch(chunk, timeoutMs); ... }` | `fetchQuotesViaChart` verarbeitet Symbol-Chunks (25 Symbole je Batch) nacheinander per `await`, obwohl jeder `fetchSparkBatch`-Call ein unabhängiger HTTP-Request an einen anderen Symbol-Ausschnitt ist und die Ergebnisse nur gesammelt werden. `TICKER_SYMBOLS` (`server/routes/stocks.js`) hat bereits 55+ Einträge (plus Portfolio-Erweiterung), sodass dieser Fallback-Pfad garantiert ≥3 Chunks sequenziell abarbeitet — im Worst Case (Yahoo down/langsam) addieren sich bis zu 3× `timeoutMs` (Default 4000ms) auf ~12s Latenz statt ~4s bei Parallelisierung. | Chunks mit `Promise.all(chunks.map(c => fetchSparkBatch(c, timeoutMs)))` parallel abfragen und die Teilergebnisse danach zusammenführen, statt sie einzeln zu awaiten. |
| MEDIUM | server/stock-providers/stooq.js:173-178 | `for (let i = 0; i < yahooSymbols.length; i += STOOQ_BATCH_SIZE) { const chunk = yahooSymbols.slice(i, i + STOOQ_BATCH_SIZE); const { quotes, errors } = await fetchStooqBatch(chunk, timeoutMs); ... }` | Gleiches Muster wie in `yahoo-chart.js`: `fetchQuotesViaStooq` fragt Symbol-Chunks (25 je Batch) nacheinander ab, obwohl jeder `fetchStooqBatch`-Call ein unabhängiger HTTP-Request an stooq.com ist. Bei 55+ Ticker-Symbolen entstehen auch hier mind. 3 sequenzielle Requests in diesem zweiten Fallback-Pfad, was die Gesamtlatenz unnötig erhöht. | Analog zu `yahoo-chart.js`: Chunk-Fetches mit `Promise.all` parallelisieren statt sie in der for-Schleife nacheinander zu awaiten. |
| MEDIUM | server/lol-match-checker.js:132-143 | `for (const matchId of matchIds) { const matchDetails = await getCachedMatchDetails(matchId, matchDetailsCache); ... if (matchEndMs && matchEndMs > createdAtMs) { selectedMatchId = matchId; selectedMatchDetails = matchDetails; } }` | `selectResolvingMatchForBet` ruft für jede der bis zu `MATCH_DETAILS_SCAN_LIMIT` `matchIds` sequenziell die externe Riot-API auf. Jeder Abruf ist unabhängig vom vorherigen — erst nach der Schleife wird synchron verglichen, welcher Match der "letzte Treffer" ist. Läuft bei jedem manuellen Bet-Check und jeder Timeout-Auflösung und summiert dabei die Riot-API-Latenz aller Scans statt sie zu parallelisieren — spürbare UX-Verzögerung beim manuellen Check. | Alle `matchIds` mit `Promise.all(matchIds.map(id => getCachedMatchDetails(id, matchDetailsCache)))` parallel abrufen und danach synchron (ohne await) über das Ergebnis-Array iterieren, um weiterhin den "letzten Treffer in matchIds-Reihenfolge" zu bestimmen. |
| LOW | server/room-manager.js:134-138 | `const { bump } = await import('./achievements.js'); for (const p of room.game.players) { await bump(p.name, 'mae_rounds', 1).catch(() => {}); }` | `awardPotAndEndGame` bumpt den Achievement-Counter `mae_rounds` für jeden Mitspieler (bis zu 6 in einem Mäxchen-Raum) nacheinander per `await`. Jeder Aufruf betrifft einen anderen `playerName` und verschluckt Fehler bereits einzeln — es besteht keine Abhängigkeit zwischen den Iterationen. | `await Promise.all(room.game.players.map(p => bump(p.name, 'mae_rounds', 1).catch(() => {})))` statt sequenzieller for-Schleife verwenden. |
| LOW | server/handlers/brain-versus.js:161-166 | `for (const g of data.games) { if (g && VALID_BRAIN_GAME_IDS.includes(g.gameId)) { ... const res = await updateGameLeaderboard(g.gameId, name, s); ... } }` | Beim täglichen `brain-submit-score` werden bis zu 5 Mini-Game-Ergebnisse nacheinander per `await` an `updateGameLeaderboard` übergeben. Jeder Aufruf betrifft ein anderes `(player_id, game_id)`-Paar mit eigener Transaktion (`server/brain-leaderboards.js:108-139`) und ist unabhängig von den übrigen Iterationen. | Leaderboard-Updates mit `Promise.all(data.games.filter(...).map(g => updateGameLeaderboard(g.gameId, name, s)))` parallel ausführen statt seriell in der for-Schleife. |
| LOW | server/cleanup.js:6-52 | `export function startPeriodicCleanup(io) { setInterval(() => { ... for (const [id] of io.sockets.sockets) {...} ... }, 5 * 60 * 1000); }` | Genau wie beim bereits dokumentierten Ticker-Refresh-Interval (server/index.js:210-218) wird auch hier das `setInterval`-Handle nirgends gehalten und nie per `clearInterval` beendet. Es gibt weder eine exportierte `stopPeriodicCleanup`-Funktion noch wird dieser Timer in `gracefulShutdown()` (`server/index.js:225-233`, ruft nur `stopMatchChecker()`/`stopKeepAlive()` auf) referenziert — der 5-Minuten-Cleanup-Timer läuft nach SIGTERM/SIGINT unkontrolliert weiter, solange `server.close()` noch auf offene Verbindungen wartet. | Interval-Handle in einer Modulvariable speichern und eine `export function stopPeriodicCleanup() { clearInterval(handle); }` bereitstellen, die analog zu `stopMatchChecker()`/`stopKeepAlive()` aus `gracefulShutdown()` aufgerufen wird. |
| MEDIUM | server/__tests__/crash.test.js:98-115 | `expect(rtp).toBeGreaterThan(expectedRtp - 1.5); expect(rtp).toBeLessThan(expectedRtp + 1.5);` | `npm test` schlägt reproduzierbar fehl: Der Test "per-target RTP is approximately..." zieht bei jedem Lauf 200.000 echte (kryptografisch zufällige, ungeseedete) `sampleCrashMultiplier()`-Werte pro Cash-out-Target und vergleicht den RTP gegen eine feste ±1.5-Prozentpunkte-Toleranz. In 2 von 2 lokalen Testläufen schlug die Assertion mit unterschiedlichen Werten fehl (97.69 % bzw. 94.15 % bei erwarteten 96 %). Für hohe Targets (z.B. t=10) liegt die statistische Standardabweichung des RTP bei n=200.000 bereits bei ca. 0.66 Prozentpunkten, sodass eine fixe ±1.5-Grenze über vier Targets hinweg eine nicht vernachlässigbare Fehlschlagsrate hat (Monte-Carlo-Rauschen, kein Code-Bug) — in CI führt das zu gelegentlichen, nicht reproduzierbaren rot/grün-Wechseln ohne echten Regressionsgrund. | Stichprobenanzahl je Target erhöhen und/oder die Toleranz pro Target an die tatsächliche Standardabweichung anpassen (z.B. ±4-5 Sigma statt fixer ±1.5 Prozentpunkte für alle Targets), oder `sampleCrashMultiplier` für Tests mit einer seedbaren PRNG-Injection versehen, damit das Ergebnis reproduzierbar statt bei jedem Lauf variabel ist. |
| MEDIUM | server/handlers/loop-machine.js:222 (i.V.m. server/handlers/achievements.js:45, server/__tests__/loop-machine.test.js:17-23) | `notifyUnlocks(io, onlinePlayers, player.name, unlocks);` → `const sock = io.sockets.sockets.get(socketId);` | Beim `npm test`-Lauf wirft `loop-toggle-cell` im Test "should accept toggle-cell for 808kick" eine unbehandelte Exception (`Cannot read properties of undefined (reading 'sockets')`), weil der Mock-`io` in `loop-machine.test.js` (Zeile 17-23) kein `sockets.sockets` besitzt, das `notifyUnlocks()` erwartet. Der Fehler wird vom äußeren try/catch des Handlers (Zeile 226) nur per `console.error` geloggt, der Test selbst schlägt NICHT fehl, da keine Assertion danach greift — das Achievement-Unlock-Handling ist damit faktisch ungetestet. Der Fehler tritt zudem nur beim ERSTEN `loop_cells`-Unlock auf, weil `memoryProgress`/`memoryUnlocks` in `achievements.js` Modul-Singletons sind, die zwischen den `it`-Blöcken derselben Datei nicht zurückgesetzt werden — das Testergebnis hängt dadurch von der Ausführungsreihenfolge ab. | Mock-`io` in `loop-machine.test.js` um `sockets: { sockets: new Map([['test-socket-1', socket]]) }` ergänzen, damit `notifyUnlocks` real durchläuft, plus eine Assertion auf `achievement-unlocked`; perspektivisch eine exportierte Reset-Funktion für `memoryProgress`/`memoryUnlocks` ergänzen, damit Tests nicht von der Ausführungsreihenfolge abhängen. |
| CRITICAL | package.json:33 | `"vitest": "^4.0.18"` | `npm audit` meldet für die installierte `vitest@4.0.18` eine kritische Schwachstelle (GHSA-5xrq-8626-4rwp): Bei laufendem Vitest-UI-Server kann eine beliebige Datei gelesen und ausgeführt werden. Das Projekt nutzt laut `package.json`-Scripts nur `vitest run`/`vitest` (kein `--ui`), wodurch das konkrete Angriffsszenario hier nicht aktiv genutzt wird — dennoch ist die installierte Version im offiziell als kritisch eingestuften, verwundbaren Bereich (`>=4.0.0 <4.1.0`), und ein Entwickler, der lokal `vitest --ui` startet, wäre exponiert. Ein Fix ist ohne Breaking Changes innerhalb der bestehenden `^4.0.18`-Range verfügbar (`npm audit fix` hebt auf `4.1.10`). | `npm audit fix` ausführen bzw. `vitest` auf `^4.1.10` oder neuer anheben. |
| HIGH | package.json:25 | `"express": "^4.18.2"` | Die aufgelöste `express@4.22.1` hängt transitiv von einer verwundbaren `path-to-regexp <0.1.13` ab (GHSA-37ch-88jc-xwx2, High: ReDoS über mehrere Routenparameter) sowie von `qs` 6.7.0-6.15.1 (GHSA-w7fw-mjwx-w883, Moderate: DoS via `arrayLimit`-Bypass beim Comma-Parsing) über `body-parser`. Da Express diese Query-/Routen-Parsing-Bibliotheken für jeden eingehenden HTTP-Request verwendet, ist dies eine produktionsrelevante Angriffsfläche (z.B. präparierte Query-Strings gegen `server/routes/*.js`). Ein Fix ist innerhalb der bestehenden `^4.18.2`-Range verfügbar. | `npm audit fix` ausführen (hebt `express` auf `4.22.2`, `qs`/`body-parser`/`path-to-regexp` auf die gepatchten Versionen an) bzw. Lockfile aktualisieren und erneut `npm audit` prüfen. |
| HIGH | package.json:28 | `"socket.io": "^4.7.2"` | Die aufgelöste `socket.io@4.8.3`-Kette hängt von verwundbaren `ws` 8.0.0-8.20.1 (GHSA-58qx-3vcg-4xpx: uninitialisierte Speicherfreigabe; GHSA-96hv-2xvq-fx4p: Memory-Exhaustion-DoS über winzige Fragmente) über `engine.io`/`socket.io-adapter` sowie von `socket.io-parser` 4.0.0-4.2.5 (GHSA-677m-j7p3-52f9, High: unbegrenzte Anzahl Binär-Attachments) ab. Da Socket.IO der zentrale Echtzeit-Transport ist, mit dem jeder Client direkt verbindet, ist dies produktionsrelevant — ein böswilliger Client könnte über manipulierte WS-Frames/Binär-Attachments einen DoS gegen den Server auslösen. Ein Fix ist innerhalb der bestehenden `^4.7.2`-Range verfügbar. | `npm audit fix` ausführen (hebt `ws`, `engine.io`, `socket.io-adapter`, `socket.io-parser` auf die gepatchten Versionen an) bzw. Lockfile aktualisieren und erneut `npm audit` prüfen. |

## Nachtrag Stichprobe (Zufalls-Sample von 10 abgehakten Dateien)

Stichprobenartige Re-Review von 10 zufällig ausgewählten, bereits abgehakten Dateien aus `REVIEW_PROGRESS.md` (mind. 2× server/ Kernmodule, mind. 2× server/handlers/): `server/pictochat-store.js`, `server/lol-betting.js`, `server/handlers/brain-versus.js`, `server/handlers/roulette.js`, `shared/js/socket-init.js`, `shared/js/achievement-toast.js`, `games/tierlist/js/game.js`, `games/roulette/js/game.js`, `server/cleanup.js`, `games/strictbrain/js/game.js`.

| Severity | Datei:Zeile | Code-Zitat | Problem | Fix-Vorschlag |
|----------|-------------|------------|---------|----------------|
| MEDIUM | server/handlers/brain-versus.js:321 | `socket.on('brain-versus-score-update', (data) => { try {` | **Nachtrag Stichprobe.** Im Gegensatz zu JEDEM anderen Socket-Handler in dieser Datei (`brain-get-leaderboard`, `brain-submit-score`, `brain-training-score`, `brain-versus-create`, `-join`, `-start`, `-finished`, `-leave` — alle beginnen mit `if (!checkRateLimit(socket)) return;`) fehlt genau bei `brain-versus-score-update` dieser Check komplett. Der Client (`games/strictbrain/js/game.js:1148-1153`, `versusScoreUpdate()`) ruft dieses Event bei jeder Punktänderung auf; ein manipulierter Client kann es über die DevTools-Konsole in einer engen Schleife beliebig oft pro Sekunde feuern. Jeder Aufruf löst synchron `io.to(room.code).emit('brain-versus-scores', ...)` aus — ein Broadcast an den ganzen Room. Ohne jegliches Rate-Limit kann ein böswilliger Spieler damit den eigenen Socket-Traffic und den des Gegners fluten (Message-Flood-DoS), ohne dass der sonst konsequent durchgesetzte `checkRateLimit`-Schutz greift. | `if (!checkRateLimit(socket)) return;` als ersten Check im `brain-versus-score-update`-Handler ergänzen, analog zu allen anderen Handlern in derselben Datei. |
| MEDIUM | games/roulette/js/game.js:348-357,424-434 | `async function handleSpin() { ...; isSpinning = true; spinBtn.disabled = true; ...; socket.emit('roulette-spin', { bets: activeBets }); }` | **Nachtrag Stichprobe.** Nach dem Absenden von `roulette-spin` wird `isSpinning` (und damit die deaktivierten Buttons/„Spinning…“-Status) ausschließlich durch die Server-Antworten `roulette-result` oder `roulette-error` zurückgesetzt. Die Datei registriert an keiner Stelle einen `socket.on('disconnect', ...)`-Handler und es gibt keinerlei Client-seitiges Timeout. Bricht die Verbindung nach dem Emit ab (Netzwerkaussetzer, Tab kurz inaktiv, Server-Neustart während der Anfrage) oder geht die Server-Antwort anderweitig verloren, bleibt die Spin-UI dauerhaft gesperrt („Spinning…“, Buttons disabled) — der Spieler kann nur durch manuelles Neuladen der Seite wieder spielen, obwohl serverseitig kein Schaden entsteht (Wallet-Transaktion ist atomar). | Einen `socket.on('disconnect', ...)`-Handler ergänzen, der `isSpinning = false` setzt und die Buttons wieder freigibt, sowie zusätzlich ein Client-seitiges Timeout (z.B. 8-10s) nach `roulette-spin`, das bei ausbleibender Antwort denselben Reset auslöst und einen Fehler anzeigt. |
| LOW | games/roulette/js/game.js:246 | `const totalDelta = finalAngle - ((startAngle - 2 * Math.PI * Math.floor(startAngle / (2 * Math.PI))));` | **Nachtrag Stichprobe.** Toter Code: `totalDelta` wird berechnet, aber im weiteren Verlauf von `spinWheelTo()` nie gelesen (die eigentliche Animation nutzt nur `startAngle`/`finalAngle` direkt in der `tick()`-Funktion). Kein funktionaler Bug, aber Hinweis auf unvollständiges Refactoring (vergleichbar mit den bereits dokumentierten Dead-Code-Funden Zeile 55/80 dieser Tabelle). | Die ungenutzte `totalDelta`-Zeile entfernen. |

**Ergebnis der Stichprobe:** Bei 7 der 10 Dateien (`server/pictochat-store.js`, `server/lol-betting.js`, `server/cleanup.js`, `shared/js/socket-init.js`, `shared/js/achievement-toast.js`, `games/tierlist/js/game.js`, `games/strictbrain/js/game.js`) deckte der bestehende Review bereits alle bei dieser Prüfung gefundenen relevanten Punkte ab (u.a. via Findings zu `server/handlers/pictochat.js:184`/`server/pictochat-store.js:24`, `server/lol-betting.js:6-39`, `server/cleanup.js:6-52`, `games/tierlist/js/game.js:254-265`/`358-380`/`158,171`, `games/strictbrain/js/game.js:1148-1158`/`1100-1101`/`1011-1017`/`985-989`). Bei den restlichen 3 Dateien (`server/handlers/brain-versus.js`, `games/roulette/js/game.js` doppelt) wurden die oben ergänzten, bislang nicht dokumentierten Findings festgestellt.
